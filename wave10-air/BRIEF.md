# Wave 10. Sunday Drive Air

Andrew (2026-09-03). The postcard should recede as one volume. Live stills already look like a quiet woodie. Fog, key, clouds, tires, and a second barn still fight that still.

Worktree: `/tmp/sunday-drive-cozy-air` on `wave/10-cozy-air`.
Live: https://sunday-drive-ten.vercel.app
Channel: Sunday Drive.

## Thesis

The postcard is one Air. Glow is the hue. The visible disc is the direction. Linear fog is the envelope. Sky, fog, background, clouds, key, hemi ground, grass tint, and cookie color are reads of that sample.

## Ask

1. Score one Air per frame (`scoreAir`) and paint it (`paintAir`). Do not grow the `createWorld` public surface.
2. Recede clouds into the same envelope. Seat the wagon and cookie on `DECK`. Plant one hero. Dim the night vignette from `look.night`.
3. Capture `?phase=0.47` and `?phase=0.72`. `npm run build` must pass.

Hold: clip-safe corridor, FOV 50, mute M, Less motion on juice only, ten destinations, wave 8 HUD. No composer, bloom, gobo, volume clouds, cabin interior, new destinations, or FOMO chrome.

Draft only. Do not merge. Do not deploy.
