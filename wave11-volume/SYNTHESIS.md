# Wave 11 synthesis

Base: private builders plus silhouette recut.
Graft: hill melt shader and night dim on land `uColor`.
Rejected: shader-only on the old sphere hills and puff clouds. Wave 10 already did that. The stills still read as a toy diorama.

Public surface of `createWorld` is unchanged.
